name: test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: test
    steps:
    - uses: actions/checkout@v5
    - uses: actions/setup-node@v6
      with:
        node-version: 22
    - run: npm ci
    - run: npm run build
    - run: npm run test
